import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.interordi.tango',
  appName: 'Tango Dico',
  webDir: '/opt/homebrew/var/www/tango.interordi.com/'
};

export default config;
